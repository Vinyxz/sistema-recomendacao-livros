{
  "name": "frontend",
  "version": "1.0.0",
  "description": "Sistema de Recomendação de Livros",
  "main": "index.js",
  "scripts": {
    "start": "live-server",
    "build": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.9.0"
  },
  "engines": {
    "node": ">=12.0.0"
  },
  "license": "ISC"
}
